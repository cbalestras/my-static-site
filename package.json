{
  "name": "static-site-generator",
  "version": "1.0.0",
  "description": "A simple static site generator",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve dist"
  },
  "dependencies": {
    "marked": "^4.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
} 